{
  "name": "wa-metro",
  "version": "1.1.0",
  "description": "Simple event scheduler for web audio api",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js --standalone WAMetro > build/wa-metro.js",
    "minify": "browserify index.js --standalone WAMetro | uglifyjs > build/wa-metro.min.js",
    "test": "mocha"
  },
  "author": "zya",
  "dependencies": {
    "webworkify": "^1.0.2"
  },
  "devDependencies": {
    "browserify": "^9.0.8",
    "chai": "^2.2.0",
    "mocha": "^2.2.4",
    "uglify-js": "^2.4.21"
  },
  "keywords": [
    "web-audio",
    "web audio api",
    "scheduling",
    "events",
    "metronome"
  ]
}
